import { SelectedTypePipe } from './selected-type.pipe';

describe('SelectedTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedTypePipe();
    expect(pipe).toBeTruthy();
  });
});
